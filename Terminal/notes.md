# Terminal

A text input and output environment.

### Usage of terminal
1. download packages
2. git
3. programming
4. speed


## Different Terms

- Command Line: any interface that ls used by entering texual commands(gen. Windows centric)
- Terminal: Thls ls a type of command line(gen. Max centric)
- Console - A command-line interface used to work with your computer
- Shell: A program running on terminal
- Bash: A popular shell on Mac OS/Linux
- Z Shell: Another shell(default)

## Basics

- ls = llst files(Show my files)
- pwd = print working directory (where am I?)
- clear = clear screen

## Navigation
Inside and outside directories

- cd = change directory

cd Desktop or cd Des* or cd Des tab(button click)

cd Javascript/JS - Arrays

cd .. = back button
cd ../.. = double back


## Paths
Absolute and Relative

cd Desktop/Delta (relative path)
cd /Users/shradhakhapra/Desktop/Delta (Absolute Path starts with slash)

/ = Root directory
~ = Home directory

## Making Directories

mkdir = make directory/folder

## Flags
Flags are characters that we pass with commands to modify their behaviour

### manual Command

ls --help: give ifo about command(for any command)
mkdir --help: give info about mkdir command

with Flags 
ls -l
ls -a(hidden folders)
ls -la

- to quit press q

## Touch Command
Used to create files

touch index.html
touch app.js
touch abc.txt

## Deleting Files & Folders
(Sensitive/No recycle bin)
rm: removes files
rmdir: removes empty folders
rm -rf: removes any folders (r is recursive delete and f is forced. Remember it as remove with force)